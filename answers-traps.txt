1. Which registers contain arguments to functions? For example, which register holds 13 in main's call to printf?

Register a2 will hold 13 in main's call to prinf.
Registers prefixed with a hold function arguments.

2. Where is the call to function f in the assembly code for main? Where is the call to g? (Hint: the compiler may inline functions.)

Function call to f is computed implicitly as we add the immediate value 12 to a1.
Function call to g occurs directly after f, and runs sequentially without jumping

3. At what address is the function printf located?

000000000000063e

4. What value is in the register ra just after the jalr to printf in main?

It is PC + 4, which essentially points to the next instruction within the stack frame as each RV64 instruction is 32-bits.

5. What is the output? Here's an ASCII table that maps bytes to characters.

It prints "HE110 World". If we flip to big-endian, we get the opposite for the %s, but not the opposite for %x because the value lives in the register.

6. In the following code, what is going to be printed after 'y='? (note: the answer is not a specific value.) Why does this happen?

It is some random number, which happens as printf loads a2 to get the second argument but it does not exist actually.